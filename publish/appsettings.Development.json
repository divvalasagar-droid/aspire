{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },

  // ── Local Dev: Application Insights ──────────────────────────
  // Paste your connection string here for local testing,
  // or set it as an environment variable instead.
  // "APPLICATIONINSIGHTS_CONNECTION_STRING": "InstrumentationKey=...;IngestionEndpoint=...",

  // ── OTLP export is disabled by default (no Aspire Dashboard) ─
  // Uncomment only if you are running an OTLP-compatible backend locally.
  // "OTEL_EXPORTER_OTLP_ENDPOINT": "http://localhost:18889",
  // "OTEL_EXPORTER_OTLP_HEADERS": "",

  "OTEL_SERVICE_NAME": "AspireTelemetryApp-Dev",
  "OTEL_SERVICE_VERSION": "1.0.0-dev"
}
